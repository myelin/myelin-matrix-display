ATTINY85 LED strand tester
==========================

* Install the patched version of avrdude: `(cd ../avrdude; sudo make install)`
* Connect a SparkFun FTDI Basic Breakout board to your ATTINY85 (TX=MOSI, RX=MISO, SCK=CTS, RESET=DTR)
* Run: `make program`
